#include<stdio.h>
typedf struct student
{
	int num;
	struct student* next;
}LNode ,*LinkList;
LinkList creat(void)
{
	LinkList head;
	LNode* p1, * p2;
	char a;
	head = NULL;
	a = getchar();
	while (a != '\n')
	{
		p1 = (LNode*)malloc(sizeof(LNode));
		
	}
}
int main()
{

	return 0;
}